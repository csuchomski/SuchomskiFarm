/** Accent colour on an ear tag's left edge. Currently only "herd" is used —
 * withdrawal and at-risk need herd.treatments and per-animal cost data,
 * neither of which has rows yet. */
export type TagAccent = "herd" | "guernsey" | "withdrawal" | "at-risk";
